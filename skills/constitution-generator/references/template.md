# AGENTS.md 分层文档体系模板（章节骨架）

> 本文件定义 `constitution-generator` 生成**分层宪法文档体系**的必含章节骨架与逐章填写规则：
> - 根 `<根>/AGENTS.md` 为**定位层**（模板一）——只做项目定位、一层目录地图、子项目宪法索引与跨切总则；
> - 每个技术子项目 `<子项目>/AGENTS.md` 为**子宪法**（模板二）——承载该子项目的全部宪法深度；
> - **整仓单应用**（依赖清单位于仓库根、无并列技术子目录）时只产出一份子宪法，落 `<根>/AGENTS.md`，省略模板一。
>
> 生成时逐章套用，禁止缺章、禁止改变章节编号结构。方括号 `[...]` 为占位说明，生成时必须替换；**信息不可得时允许 `{待调研项}` 占位（禁止编造），须在 TASK.md 登记表（无 TASK.md 时子宪法附录）集中登记**；`※` 标注为规则性说明，不写入最终文档。

---

# 模板一 · 根 AGENTS.md（定位层）

> 角色定位：**仓库导览层**——只做整个仓库的地图与索引，深度一律指向子宪法；在子项目内部工作时读该子项目的 AGENTS.md 而非本文档。正文 ≤ 200 行（默认，见 SKILL.md §二「行数与去重要求」）。

## 文档头（生成文档最顶部）

```markdown
# AGENTS.md

> 本文件为 AI 编码代理提供仓库级指引，是**定位层**：只描述项目定位、**根目录一层**结构与子项目宪法索引，不承载任何子项目内部的深度约束。
> **强制阅读路由**：写后端代码必读 [backend/AGENTS.md](backend/AGENTS.md)，写前端代码必读 [frontend/AGENTS.md](frontend/AGENTS.md)；本文档只是地图与索引，子项目内的任何深度约束以子宪法为准。
> 子项目宪法索引（按实际子项目列全）：
> - [backend/AGENTS.md](backend/AGENTS.md) —— {一句话深度范围}
> - [frontend/AGENTS.md](frontend/AGENTS.md) —— {一句话深度范围}
> {若引用全局约束文件} 注释 / 日志 / 测试规范见全局 `{全局约束文件路径}`（§…），本项目不重复正文，强制生效。
```

※ 全局约束文件引用与各子宪法保持同源同章节号（多处引用属「索引」，不属「重复正文」）。
※ **强制阅读路由为必含项**：逐子项目写明「写 {该子项目技术域} 代码必读 <子项目>/AGENTS.md」，与第 5 章索引一一对应。

### 1. 项目定位

2–4 句：一句话业务定位 + 各子项目角色概述（后端做什么、前端做什么、外部接入等）。只写"是什么"，不写技术细节（技术细节归子宪法）。

### 2. 运行形态（可选：多服务协作时）

表：服务 | 端口/入口 | 职责。原则级描述协作关系（谁代理谁、单一公网入口、内部端口不外暴等），运行与路由细节归对应子宪法。

### 3. 仓库地图（**仅一层**）

```markdown
{项目名}/
├── Makefile                    # 根编排命令（见下「命令总览」）
├── config.example.yaml         # 模板 → 复制为 config.yaml（根运行时配置，实文件 gitignore）
├── AGENTS.md / CLAUDE.md       # 定位层 / 其 md 链接索引（CLAUDE.md 为配套生成）
├── TASK.md                     # 待办与登记台（{待调研项} / 待决策 / TODO 工单）
├── CHANGELOG.md                # 工程变更记录（宪法修订先记此处再改正文）
├── backend/                    # Python 后端 —— 规范见 backend/AGENTS.md
├── frontend/                   # Next.js 前端 —— 规范见 frontend/AGENTS.md
├── docker/                     # 容器编排与反向代理配置
├── scripts/                    # 根编排脚本
├── docs/                       # 跨切文档（agmds-research 调研报告在此）
└── tests/                      # 根级测试
```

※ **只画一层**：每个子目录一行职责注释；技术子项目行尾必标「规范见 <子项目>/AGENTS.md」；**禁止展开子项目内部目录**（子项目内部 tree 归其子宪法 C.3 目录结构）。
※ 运行时配置的跨切说明（配置放哪、模板复制关系、经谁编辑）可在此补 2–3 句，配置结构细节归对应子宪法。

### 4. 命令总览（根 vs 子项目）

根编排命令代码块（全栈起停 / 安装 / 检查 / 部署，含一句注释）+ 一句口诀：**根命令 = 整个应用；子项目内命令 = 单模块工作**。子项目内命令各举 1–2 例并指向子宪法，禁止展开子项目全量命令清单。

### 5. 子项目宪法索引（必含）

| 子项目 | 宪法 | 深度范围（一句话） |
|---|---|---|
| backend/ | [backend/AGENTS.md](backend/AGENTS.md) | {如：运行时与路由、架构分层、持久化与迁移、配置系统、测试布局} |
| frontend/ | [frontend/AGENTS.md](frontend/AGENTS.md) | {如：页面结构、状态与数据流、视觉令牌、代码风格与命令} |

※ 路径与深度范围描述必须与实际落盘的子宪法一一对应（SKILL.md §四.5 审核专项）；非技术目录（docs/、docker/、scripts/）不入本索引。
※ 本索引是文档头「强制阅读路由」的展开：在该子项目内写代码前**必读**其宪法。

### 6. 跨切约定

repo 级原则条目，每条 1–3 行，**只写总则**，细节归对应子宪法：
- **配套文件职责** —— `TASK.md` 待办与登记台（{待调研项} / 待决策 / TODO 工单，回填后删除对应行）；`CHANGELOG.md` 工程变更记录（宪法修订与重大变更，**先记变更再改正文**）；`docs/` 过程文档（含 agmds-research 调研报告）
- 文档同步策略（用户可见变更更 README，架构/开发规范变更更对应 AGENTS.md，同一变更集内完成）
- 全栈门禁总则（合入主干前各子项目 lint + format + 测试全过；各语言工具链与命令归子宪法）
- 跨子项目协作契约（如统一 API 前缀、contracts/ 目录、共享数据契约）——细节在哪个子项目就指向哪个子宪法

### 7. 去哪里深入

列表：各子项目开发 → 对应子宪法（写代码前必读）；待办登记 → TASK.md；变更记录 → CHANGELOG.md；安装 / 贡献 / 安全策略 / 发布 → 对应根级文档（README.md、CONTRIBUTING.md、SECURITY.md 等，按项目实存列）。

---

# 模板二 · <子项目>/AGENTS.md（子项目宪法）

> 角色定位：**子项目宪法**——只存工程原则与约束；业务功能设计与数据契约一律归 `specs/`，本文件只留职责引用行；变更记录一律外置 `CHANGELOG.md`，正文不设变更控制章节。正文 ≤ 500 行（默认，见 SKILL.md §二「行数与去重要求」）。Part B（架构分层）按子项目实际整体省略或保留；Part C 必有。

## 文档头（生成文档最顶部）

```markdown
# {子项目名} 宪法

> 本子项目最高规范：**只存工程原则与约束**。功能实现与业务数据契约见代码与 `specs/` 设计文档，待办登记见 `TASK.md`，工程变更记录见 `CHANGELOG.md`（宪法修订先记 CHANGELOG 再改正文），本文档均不重复。
> 任何与本文档冲突的代码或设计不得合入主分支。
> 仓库定位层（项目定位、一层目录地图、跨切总则）见根 `../AGENTS.md`，本文档只管本子项目深度，跨切总则不在此重复。
> {若引用全局约束文件} 注释 / 日志 / 测试覆盖规范见全局 `{全局约束文件路径}`（§一/§二/§六），本项目不重复正文，强制生效。

**配套文件职责**：
- `specs/` —— 功能设计与业务数据契约（数据模型、图模型、状态机、字段口径、业务算法）的唯一权威来源，宪法不重复；
- `TASK.md` —— 待办与登记台：调研不可得项（{待调研项}）、待决策项、TODO 工单；回填/完成后删除对应行；
- `CHANGELOG.md` —— 工程变更记录（根级，各子宪法共用）：宪法修订与重大工程变更追加记录（日期 · 变更简述 · 原因），先记变更再改正文；
- {其他项目文件（如 docs/）} —— 过程文档。

**三段结构**：Part A {子项目技术栈} 通用 / Part B 架构分层 / Part C {子项目名} 实际。
```

※ 无 TASK.md / CHANGELOG.md 的项目省去对应职责行；无 `specs/` 设计文档时头注首行改引「{设计文档位置}」。
※ 头注三要素：只存工程原则约束的定位声明、违者不合入主分支、引用全局规范（存在才写）。Part 编号固定（A 通用 / B 架构分层 / C 项目实际）；省略段时编号不重排。
※ 前端子宪法实例化：Part A = 前端技术栈通用（框架版本、组件栈、视觉令牌隔离——多端时禁止混用）；A.3 API 设计整节省略；Part B = 页面 / 组件 / 状态管理的架构分层；lint / format / 测试门禁按 C.4 落地。

---

## Part A — {子项目技术栈} 通用规范

### A.1 编码约束（调研主题：语言 + 框架）

1. 编码/行尾/版本约束（如 UTF-8、LF、语言版本下限）
2. 类型要求（type hint / 严格类型模式，按调研结果）
3. 异步/并发界限（async 约束、禁止异步上下文内同步阻塞等）
4. 输出约束（日志 vs print、禁止裸输出）

### A.2 配置管理（调研主题：语言 + 框架 / 部署）

1. 配置分层（非敏感配置文件 + 敏感 env 分离）
2. 敏感值占位与注入方式
3. 模板文件要求（如 `.env.example` / `config.example.yaml`）

### A.3 API 设计（调研主题：语言 + Web 框架；无对外 API 的子项目整节省略）

1. 版本化前缀
2. 请求/响应契约（框架的契约模型，禁止裸 dict 等）
3. 统一错误响应格式与 HTTP 状态码语义

### A.4 数据库操作（调研主题：ORM + 迁移 + 数据库；无 DB 则整节省略）

1. 表结构变更强制迁移（含 upgrade/downgrade 或等价机制）
2. 会话/连接生命周期约定（异步 session、禁止请求内同步等）
3. 查询与索引约束（禁止全表扫描等）

### A.5 基础设施生命周期（调研主题：缓存 + 消息队列 / 图数据库 / 部署；无则省略）

1. 进程级单例 / 显式生命周期（fail-fast）
2. 依赖获取方式（工厂式注入，禁止直接 import 单例）
3. 线程池 / 异步调度约束
4. 后台任务强引用 / 生命周期管理
5. 消息流与特殊存储模式（消费组、重试、幂等、取消、连接生命周期——按调研结果）

### A.6 注释 / 日志 / 测试（引用全局，不重复）

```markdown
见 `{全局约束文件路径}` §…。本项目强制生效。
```

※ 无全局约束文件可引用时，本段按调研主题「构建 + 测试 + CI 门禁」产出条目直接写入宪法。

---

## Part B — 架构分层（子项目无分层架构则整段省略）

### B.1 目录职责边界

表格：目录 | 职责边界。

### B.2 层级依赖（强制）

- 依赖方向图（text 或 mermaid）
- 禁止项：跨层调用、反向依赖、循环 import、事务边界归属

### B.3 运行时原则（核心运行循环：工作流/服务整体如何运转）

按调研到的框架运行时特性写原则级条目（如并发控制、取消与回滚、重试、过期清理等——以调研结果为准，不臆造）。

### B.4 {外部能力}网关（如 LLM / 媒体网关；无则省略）

模型/能力注册表、路由规则、凭证约束、请求记录要求。

### B.5 横切关注点（按调研主题列出）

如安全 / 审计 / 限流 / 确认（HITL）/ 上下文管理——每条一行原则，不写实现。

---

## Part C — {子项目名} 实际

### C.1 子项目定位

一句话：本子项目在仓库中的角色 + 核心流程描述。

### C.2 技术栈选型（表：职责 | 技术 | 版本）

版本必须来自用户提供或勘察证据（与 §三 调研锁定版本一致），禁止编造。只列本子项目技术栈。

### C.3 目录结构

tree 图（**本子项目内部**：配置文件、入口、各层目录、测试目录）。根目录一层地图归定位层，不在此重复。

### C.4 永久环境约束 + 门禁落地

- 永久环境约束：构建/编码级硬约束与例外（如编码 locale 例外、依赖被阻塞的替代方案）
- 本地 hook / CI 门禁落地命令（如 `pre-commit install` + `pre-commit install --hook-type pre-push`）
- {后续实现索引：仅当用户要求时给「功能 | TASK.md 条目」对照表，索引内容不展开——详情归 TASK.md}

---

## 附录 · 待回填登记表（无 TASK.md 时的内嵌形态）

| 待回填项 | 涉及段落 | 原因 | 回填状态 |
|---|---|---|---|

※ 默认形态：登记表落 `TASK.md`（A 组调研不可得 / B 组待决策 / C 组 TODO 工单），头注「配套文件职责」指向，本附录省略；仅当项目无 TASK.md 时内嵌本附录。
※ 生成时所有 `{待调研项}` 登记；回填后删除该行或标记完成。

---

# 模板三 · 配套 CLAUDE.md（每份 AGENTS.md 一份，md 链接索引）

> 角色定位：**纯索引**——保证以 CLAUDE.md 为入口的编码工具可达宪法体系；只放 md 链接与一句话说明，**禁止写任何规范正文**（内容与 AGENTS.md 重复即违规）。与 AGENTS.md 同目录同名配套生成。

## 根 CLAUDE.md（`<根>/CLAUDE.md`）

```markdown
# CLAUDE.md

> 本文件是编码工具入口索引，不承载规范；项目宪法唯一权威来源为 AGENTS.md 体系，经以下链接进入：
> **写后端代码必读 [backend/AGENTS.md](backend/AGENTS.md)，写前端代码必读 [frontend/AGENTS.md](frontend/AGENTS.md)**。

- 根定位层（项目定位、一层目录地图、子项目宪法索引、跨切总则）：[AGENTS.md](AGENTS.md)
- 子项目宪法：[backend/AGENTS.md](backend/AGENTS.md) · [frontend/AGENTS.md](frontend/AGENTS.md)（按实际子项目列全）
- 配套文件：[TASK.md](TASK.md)（待办与登记台）· [CHANGELOG.md](CHANGELOG.md)（工程变更记录，先记变更再改正文）
```

## 子项目 CLAUDE.md（`<子项目>/CLAUDE.md`）

```markdown
# CLAUDE.md

> 本文件是编码工具入口索引，不承载规范；本子项目宪法唯一权威来源：[AGENTS.md](AGENTS.md)——**写本子项目代码前必读**。
> 仓库定位层见 [../AGENTS.md](../AGENTS.md)；根级配套文件见 [../TASK.md](../TASK.md) 与 [../CHANGELOG.md](../CHANGELOG.md)。
```

※ 链接必须用 md 语法且路径与实际文件一一对应（相对路径，随 AGENTS.md 修订同步更新）；整仓单应用时只生成根 CLAUDE.md。

---

## 填写规则（生成自查清单）

1. 子宪法只写**工程原则与约束**两类内容（定位层另含索引/地图/总则）；"怎么实现"与业务数据契约不留——归代码与 `specs/`
2. 每条条款有调研来源支撑（来源记在调研报告，宪法正文不逐条标注）
3. 版本必须来自用户提供或勘察证据，禁止编造
4. 跨主题冲突裁决：官方文档 > 厂商实践 > 社区实践
5. 引用全局约束文件时章节号以实际文件为准（先读后写）；定位层与各子宪法引用同源同章节号
6. 生成前核查：结构完整、无编造、无实现细节、无重复全局规范正文、全部 `{待调研项}` 已登记
7. **行数上限**：各文档正文 ≤ 对应上限（定位层默认 200 行 / 子宪法默认 500 行），超限必须砍重复/实现细节/功能设计
8. **无重复描述**：同一规则全篇只落一处，其余交叉引用（含对全局约束文件的引用不重复正文；定位层与子宪法之间引用不重复正文）
9. **功能设计边界**：业务数据模型、图模型节点/关系清单、状态机事件、业务计算规则等一律不写入宪法，只留指向 `specs/` 的职责行
10. **变更记录外置**：宪法正文不设变更控制/变更记录章节；修订流程 = 先记 `CHANGELOG.md` 再改正文，语义由头注「配套文件职责」承载
11. **归属规则**：约束归属其子项目宪法——前端约束不写进后端子宪法，子项目深度约束不写进定位层；跨切约定只写总则，细节归对应子宪法
12. **一层目录规则**：定位层仓库地图只画根目录一层；子项目内部结构归该子宪法 C.3 目录结构
13. **引用链一致性**：定位层索引中的路径与深度范围描述必须与实际落盘的子宪法一一对应；子宪法头注反指定位层；引用（含章节号与路径）不算重复正文，复制正文才算
14. **强制路由规则**：定位层文档头必含「写 {子项目技术域} 代码必读 <子项目>/AGENTS.md」，逐子项目列全，与第 5 章宪法索引一一对应
15. **配套索引规则**：每份 AGENTS.md 同目录配套一份 CLAUDE.md，全部用 md 链接语法索引；纯索引不写规范正文；链接路径与实际文件一一对应，AGENTS.md 路径调整时同步更新
