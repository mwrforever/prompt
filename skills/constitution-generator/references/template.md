# 分层宪法模板（章节骨架）

> 本文件是产出物**结构与约束方向的唯一权威**：模板一 = 根定位层，模板二 = 子项目宪法，模板三 = 根索引。
> **命名映射**：以 agent 模式表述（AGENTS.md 宪法实体 + CLAUDE.md 根索引）；claude 模式全局对调命名，结构与规则不变。两种宪法**必然同时生成**——一方承载实体、一方做索引；索引只落根目录一份，**禁止在子项目目录生成索引文件**（SKILL.md §一）。
> `[...]` 为占位说明，生成时替换；信息不可得写 `{待调研项}`（禁止编造，登记 TASK.md）；`※` 为填写规则，不写入最终文档。

---

# 〇、宪法基础框架（调研与合成的统一目标）

## 层级依赖

```text
根索引（<根>/CLAUDE.md，纯 md 链接，根目录仅此一份）
    └─链接可达─▶ 根定位层（<根>/AGENTS.md：定位 / 地图 / 路由 / 索引 / 效力总则 / 跨切总则）
                     └─强制阅读路由─▶ 子宪法 ×N（<子项目>/AGENTS.md：深度唯一承载）
                                          └─职责引用─▶ specs/（业务设计）· TASK.md（登记）· CHANGELOG.md（变更）
```

**权威顺序**（写入定位层 §6）：单子项目事项以其子宪法为准；跨子项目事项以定位层为准；两者未规定者遵引用的全局约束文件；全局与项目宪法冲突时项目宪法优先（更特化）。

## 必含章节与约束方向

| 文档 | 必含章节 | 约束方向 |
|---|---|---|
| 根定位层 | 文档头；1 项目定位；2 运行形态（多服务时）；3 仓库地图（仅一层）；4 命令总览；5 子项目宪法索引；6 约束效力与遵从总则；7 跨切约定；8 去哪里深入 | 地图只画一层，宪法关联目录职责必写；§6 声明全局 + 局部宪法强制生效、冲突不合入、裁决顺序；跨切只收所有子项目共有总则（含 CI 链总则）；「配套文件职责」声明唯一落点 |
| 子宪法 ×N | 文档头；A.1 编码 / A.2 配置 / A.3 API（无对外 API 省）/ A.4 数据库（无 DB 省）/ A.5 基础设施（无则省）/ A.6 注释日志测试 / **A.7 跨层数据对象与传参（必含）**；B 架构分层（无则整段省）；C.1 定位 / C.2 选型 / C.3 目录 / **C.4 常用命令（必含）** / **C.5 CI 生产落地方案（必含）** / C.6 永久环境 | A 组逐槽原则级条款（可验证、禁令表述）；**A.7 三原则 + 例外边界按本栈习语翻译**（后端 = 参数对象 / 请求 / 响应模型；前端 = interface / type / props），禁照抄抽象表述；C.5 按选定 CI 方案成文；C.2 版本只认实测 |
| 根索引 | 纯 md 链接清单 + 强制阅读路由复现 | 不写规范正文，链接与实存一一对应 |

## 调研槽位映射

- 派遣必标注：归属文档 + 目标槽位（章节号）+ 必备约束方向；报告按槽位分节，评审按槽位核对（SKILL.md §四/§六）。
- **内置条款不依赖调研**：约束效力总则、A.7 三原则与例外边界、C.4 / C.5 骨架按模板直写；调研只补本栈习语与流水线细则。
- **CI 先行**：CI 链方案先经用户确认（SKILL.md §三/§五.1），确认前不得调研、定稿其它槽位。
- 可省略槽位（A.3 / A.4 / A.5 / Part B、定位层「运行形态」）省略时编号不重排；必含槽位禁止缺章。

---

# 模板一 · 根 AGENTS.md（定位层，≤ 200 行）

> 仓库导览层：只做地图与索引，深度一律指向子宪法。

## 文档头

```markdown
# AGENTS.md

> 本文件为 AI 编码代理提供仓库级指引，是**定位层**：只描述项目定位、**根目录一层**结构与子项目宪法索引，不承载子项目内部深度约束。
> **强制阅读路由**：写任一子项目代码前必读其宪法（见「子项目宪法索引」）；子项目内的深度约束以子宪法为准。
> **约束效力**：本定位层（含跨切总则）与全部子项目宪法对所有开发行为强制生效，冲突代码 / 设计不得合入主分支（总则见 §6）。
> {若引用全局约束文件} 注释 / 日志 / 测试规范见全局 `{路径}`（§…），不重复正文，强制生效。
```

※ 路由规则句只在文档头出现一次；逐子项目路径与深度范围只在 §5 描述一次，两处不得复述。

### 1. 项目定位
2–4 句：业务定位 + 各子项目角色，不写技术细节。

### 2. 运行形态（多服务协作时）
表：服务 | 端口/入口 | 职责；原则级协作关系（如单一公网入口、内部端口不外暴），细节归子宪法。

### 3. 仓库地图（仅一层）

```markdown
{项目名}/
├── Makefile                    # 根编排命令（见「命令总览」）
├── AGENTS.md / CLAUDE.md       # 定位层 / 根索引（全项目仅此一份索引）
├── TASK.md                     # 待办与登记台（{待调研项} / 待决策 / TODO）
├── CHANGELOG.md                # 工程变更记录（先记再改正文）
├── backend/                    # Python 后端 —— 规范见 backend/AGENTS.md
├── frontend/                   # Next.js 前端 —— 规范见 frontend/AGENTS.md
├── docker/                     # 容器编排与反向代理
├── docs/
│   ├── specs/                  # 功能设计与业务数据契约（宪法只引用）
│   └── agmds-research/         # 宪法调研报告
└── scripts/                    # 根编排脚本
```

※ 每目录一行职责；技术子项目行尾必标「规范见 <子项目>/AGENTS.md」，内部结构禁止展开（归其子宪法 C.3）；宪法引用的根级目录（docs/specs/ 等）职责必写，非技术目录可下钻一层。

### 4. 命令总览（根 vs 子项目）
根命令代码块（全栈起停 / 安装 / 检查 / 部署，各带一句注释）+ 口诀：**根命令 = 整个应用；子项目内命令 = 单模块工作**。子项目命令各举 1–2 例，全量清单指向其子宪法 C.4。

### 5. 子项目宪法索引（必含）

| 子项目 | 宪法 | 深度范围（一句话） |

※ 路径与深度范围与实际落盘的子宪法一一对应（审核专项）；非技术目录不入索引。

### 6. 约束效力与遵从总则（必含，条目不随技术栈变化）

1. **强制生效**：本定位层（含跨切总则）与全部子项目宪法对一切开发行为（编码 / 设计 / 脚本 / CI / 文档）强制生效；{引用的全局约束文件} 同样强制生效。
2. **不得违背**：冲突以宪法为准，冲突产物不得合入主分支；禁止「临时 / 紧急」绕过——修宪先记 `CHANGELOG.md` 再改正文，一次性事项登记 `TASK.md` 待决策并限定范围。
3. **遵从路径**：开发任一子项目前必读其宪法；跨子项目行为同时遵守跨切总则。
4. **裁决顺序**：单子项目事项以其子宪法为准；跨子项目事项以本文档为准；未规定事项遵引用的全局约束文件；全局与本项目宪法冲突时以本项目宪法（更特化）为准。

### 7. 跨切约定
repo 级总则，每条 1–3 行，细节归对应子宪法：
- **配套文件职责**（全体系唯一声明落点）—— `TASK.md` 登记台；`CHANGELOG.md` 变更记录（先记再改）；`docs/` 过程文档
- 文档同步策略；全栈门禁总则
- **CI 链总则** —— 全项目 CI 工具与统一门禁原则（按选定方案）；各子项目流水线细则归其子宪法 C.5
- 跨子项目协作契约（统一 API 前缀、契约目录等）

### 8. 去哪里深入
各子项目开发 → §5 索引（写代码前必读其宪法）；设计与契约 → `docs/specs/`；登记 → `TASK.md`；变更 → `CHANGELOG.md`；安装 / 贡献 / 安全 → 根级实存文档（README.md、CONTRIBUTING.md 等）。

---

# 模板二 · <子项目>/AGENTS.md（子项目宪法，≤ 500 行）

> 只存工程原则与约束；业务功能设计与数据契约归 `specs/`（只留职责引用行），变更记录归 `CHANGELOG.md`（正文不设变更控制章节）。Part B 无分层架构整段省略；A.7、C.4、C.5 必有。

## 文档头

```markdown
# {子项目名} 宪法

> 本子项目最高规范：**只存工程原则与约束**。功能实现与业务数据契约见代码与 `specs/`，待办见 `TASK.md`，变更记录见 `CHANGELOG.md`（先记再改），本文档均不重复。
> 任何与本文档冲突的代码或设计不得合入主分支。
> 仓库定位层见根 `../AGENTS.md`：跨切总则不在此重复，说明性内容不复制，也不引用兄弟子宪法替代成文（与他端相同的约束在本文件完整成文）。
> {若引用全局约束文件} 注释 / 日志 / 测试覆盖规范见全局 `{路径}`（§…），强制生效。

配套文件职责（specs/ / TASK.md / CHANGELOG.md 边界）由根定位层声明，本文件只留指向、不复述。

**三段结构**：Part A {技术栈} 通用 / Part B 架构分层 / Part C {子项目名} 实际。
```

※ 头注指向按项目实际裁剪；无 `specs/` 时改引经实存探查确认的设计文档位置，禁止凭历史 / 全局记忆关联（SKILL.md §三）。
※ **整仓单应用**：本文件落仓库根，头注后增列「约束效力与遵从总则」（同模板一 §6 去掉跨子项目条款）与「配套文件职责」声明；根索引照常生成。

## Part A — {技术栈} 通用规范

### A.1 编码约束（调研：语言 + 框架）
编码 / 行尾 / 版本下限；类型要求；异步界限（禁异步上下文同步阻塞）；输出约束（禁裸输出）。

### A.2 配置管理
配置分层（非敏感文件 + 敏感 env）；敏感值占位注入；模板文件（`.env.example` 等）。

### A.3 API 设计（无对外 API 整节省略）
版本化前缀；请求 / 响应契约模型（禁裸 dict）；统一错误格式与状态码语义。

### A.4 数据库操作（无 DB 省略）
表结构变更强制迁移（upgrade / downgrade）；会话 / 连接生命周期；查询与索引约束。

### A.5 基础设施生命周期（无则省略）
进程级单例 + 显式生命周期（fail-fast）；依赖工厂注入；后台任务强引用；消息流（消费组、幂等、有界重试）。

### A.6 注释 / 日志 / 测试

```markdown
见全局 `{路径}` §…。本项目强制生效。
```

※ 无全局约束文件时，按「构建 + 测试」调研结果直接成文。

### A.7 跨层数据对象与传参约束（必含·体系内置）
前后端通用三原则 + 例外边界，**必须按本栈习语翻译后成文**（后端 = 参数对象 / 请求 / 响应模型等；前端 = interface / type / props 等），禁照抄抽象表述：

1. **参数对象化**：公开方法形参 > 3 必须参数对象整体传参，禁逐参罗列；仅业务确需灵活传参可例外。
2. **返回业务对象**：业务返回必须业务对象（禁裸 dict / Map 等无结构载体）；请求与响应分别建模，禁同一对象双向复用（A.3 省略时本条承载请求 / 响应建模）。
3. **职责隔离**：不同业务职责对象禁复用——字段全同也按职责各自建类（查询参数 ≠ 响应对象 ≠ 存储模型）；禁万能对象承载多职责、禁跨层直传。
4. **例外边界**：仅业务功能确需动态 / 灵活结构可偏离，且须能陈述业务理由；无理由的违反视为缺陷，不得合入。

## Part B — 架构分层（无分层架构整段省略）

### B.1 目录职责边界
表：目录 | 边界。

### B.2 层级依赖（强制）
依赖方向图（text）；禁跨层调用、反向依赖、循环 import；事务边界归属。

### B.3 运行时原则
核心运行循环（并发 / 取消 / 重试 / 清理）——以调研为准，不臆造。

### B.4 {外部能力}网关（无则省略）
能力注册表、路由规则、凭证约束、请求记录要求。

### B.5 横切关注点
安全 / 审计 / 限流 / HITL 等，每条一行原则，不写实现。

## Part C — {子项目名} 实际

### C.1 子项目定位
一句话角色 + 核心流程。

### C.2 技术栈选型
表：职责 | 技术 | 版本。版本只认用户输入或勘察证据，禁编造。

### C.3 目录结构
本子项目内部 tree（入口、各层目录、测试目录）；根地图归定位层，不在此重复。

### C.4 常用命令（必含）
表：命令 | 用途，覆盖打包 / 构建、测试、格式化、修复（lint --fix）、启动等全量；根命令总览只举 1–2 例指向本节。

### C.5 CI 生产落地方案（必含）
按用户选定 CI 方案成文（既有 CI 沿用补强）：
1. 工具与触发：push / PR 触发、主干分支保护要求检查全过
2. 流水线阶段与硬门禁：lint → format 校验 → 测试 → 构建 → 产物，任一失败阻断合入
3. 缓存与加速：依赖 / 产物缓存（按调研）
4. 部署与环境晋升：产物去向、环境推进规则（无部署则省略本条）

※ 与定位层 CI 链总则同源：总则在定位层，本子项目细则全文在此（宁可重复，不引兄弟子宪法）。

### C.6 永久环境约束
构建 / 编码级硬约束与例外；本地 hook 门禁命令（如 `pre-commit install`）；{按需：功能 | TASK.md 条目索引，不展开}。

## 附录 · 待回填登记表（仅无 TASK.md 时内嵌）

| 待回填项 | 涉及段落 | 原因 | 回填状态 |

※ 默认落 `TASK.md`（调研不可得 / 待决策 / TODO 工单，回填后删除），本附录省略；所有 `{待调研项}` 必须登记。

---

# 模板三 · 根索引（相反命名，纯链接）

> 与宪法实体**同时生成**：agent 模式 = `<根>/CLAUDE.md`；claude 模式 = `<根>/AGENTS.md`。只放 md 链接与一句话说明，禁止规范正文；根目录仅此一份，子项目目录禁止索引文件。

```markdown
# CLAUDE.md

> 本文件是编码工具入口索引，不承载规范；项目宪法唯一权威来源为 AGENTS.md 体系：
> **写后端代码必读 [backend/AGENTS.md](backend/AGENTS.md)，写前端代码必读 [frontend/AGENTS.md](frontend/AGENTS.md)**。

- 根定位层：[AGENTS.md](AGENTS.md)
- 子项目宪法：[backend/AGENTS.md](backend/AGENTS.md) · [frontend/AGENTS.md](frontend/AGENTS.md)
- 配套文件：[TASK.md](TASK.md) · [CHANGELOG.md](CHANGELOG.md)
```

※ 整仓单应用省去子项目宪法行；claude 模式命名整体对调；链接用相对路径、与实存一一对应，宪法路径调整时同步更新。

---

## 生成自查（落盘前逐项核对）

1. **结构完整**：必含章节齐全，省略槽位编号未重排；行数 ≤ 上限（定位层 200 / 子宪法 500）
2. **内置条款**：A.7 按本栈习语翻译（非照抄抽象表述）、双端各自成文；C.5 与选定 CI 方案一致；约束效力总则在位
3. **归属与去重**：共有总则归定位层；子项目约束各自成文宁可重复；说明性内容与「配套文件职责」不复制
4. **无编造**：条款有调研来源支撑（来源记调研报告，正文不逐条标注）；版本来自实测证据
5. **引用实存**：`specs/` 等引用经当前工作区探查确认；全部 `{待调研项}` 已登记
6. **引用链一致**：定位层索引 ↔ 子宪法实存一一对应；子宪法头注反指定位层；根索引纯链接可达、路径正确、全项目仅此一份
